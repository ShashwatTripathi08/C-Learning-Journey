#include<stdio.h>

int main(){
    int i = 10; // Declare and initialize i with 10
    int j = i; 
    // %d is called format specifier
    // %d us for int, %f is for float, %c is for char
    
    printf("The value of i is %d and the value of j is %d", i,j);

    return 0;
}